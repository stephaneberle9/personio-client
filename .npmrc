# SheetJS (xlsx) is distributed from its own CDN, not the npm registry, so the
# `xlsx` dependency is pinned to a cdn.sheetjs.com tarball (see package.json).
# npm 12 blocks remote-tarball installs by default; allow them for root-level
# direct dependencies only (not transitive packages).
allow-remote=root
